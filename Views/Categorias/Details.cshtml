@model Exemplo1EF.Models.Categoria

@{
    ViewData["Title"] = "Detalhes da Categoria";
}

<div class="container mt-5">
    <div class="card shadow-lg">
        <div class="card-header bg-info text-black">
            <h3 class="mb-0" style="color:black">Detalhes da Categoria</h3>
        </div>
        <div class="card-body">
            <dl class="row">
                <dt class="col-sm-3 fw-bold" style="color:black">@Html.DisplayNameFor(model => model.Nome)</dt>
                <dd class="col-sm-9" style="color:black">@Html.DisplayFor(model => model.Nome)</dd>
            </dl>
        </div>
        <div class="card-footer d-flex justify-content-between">
            <a asp-action="Index" class="btn btn-outline-secondary">Voltar</a>
            <a asp-action="Edit" asp-route-id="@Model?.CategoriaId" class="btn btn-primary">Editar</a>
        </div>
    </div>
</div>